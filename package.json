{
  "name": "mcprp",
  "version": "1.0.0",
  "description": "An MCP server that helps with content generation for roleplaying games",
  "main": "dist/index.js",
  "type": "module",
  "module": "esnext",
  "target": "es2017",
  "scripts": {
    "build": "npx tsc",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "start": "node dist/index.js",
    "test": "NODE_OPTIONS='--loader ts-node/esm --no-warnings' mocha test/**/*.ts"
    },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.0.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.54.0",
    "@modelcontextprotocol/sdk": "^1.12.3"
  }
}
